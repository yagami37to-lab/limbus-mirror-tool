# CODE CLEANUP REPORT v1.0.87

## 目的

依存関係の多い人格選択を小さな単位から安全に分離する。

## 今回の分割

以下を `js/identity-filter-controller.js` へ移動した。

- 人格名検索
- レアリティ検索
- キーワードの複数選択
- AND／OR検索
- 複数キーワード人格のみの絞り込み
- 条件付きキーワードを含める設定
- 検索条件の表示とリセット
- 人格候補の絞り込みと並び替え

## 接続方法

`app.js` は現在の囚人の人格候補を検索コントローラーへ渡し、絞り込み済みの候補と自由枠の表示可否を受け取る。人格の選択、代用人格、編成順、E.G.Oの状態形式は変更していない。

## 結果

- `app.js`: 約75KBから約71KBへ縮小
- 人格検索: `js/identity-filter-controller.js` 約4KBへ独立
- 人格選択と投稿保存のデータ形式は変更なし

## 次の候補

第7段階では、囚人一覧・人格カード選択・自由枠・代用人格を専用コントローラーへ分離する。
